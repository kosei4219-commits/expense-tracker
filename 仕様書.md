# Expense Tracker（支出管理アプリ） - 仕様書

## 概要

Expense Trackerは、日々の支出を簡単に記録・管理できるWebアプリケーションです。ユーザーは支出の日付、金額、カテゴリー、メモを入力することで、自分の消費行動を可視化し、カテゴリー別の支出分析を行うことができます。

データはブラウザのlocalStorageに一時保存され、Google Apps Script（GAS）を介してGoogle Spreadsheetと同期することで、データの永続化とバックアップを実現します。GitHub Pagesでホスティングすることで、誰でもアクセス可能なWebアプリケーションとして公開できます。

## 目的

- 日々の支出を手軽に記録できる環境を提供する
- カテゴリー別に支出を分類し、支出傾向を可視化する
- Google Spreadsheetと連携し、データの永続化とバックアップを実現する
- モダンで使いやすいUIで、継続的な記録を促進する
- GitHub Pagesでホスティングし、どこからでもアクセス可能にする

## 画面構成

### メイン画面（単一ページアプリケーション）

```
┌─────────────────────────────────────────────────┐
│            💰 Expense Tracker                   │
│         日々の支出を記録・管理                     │
├─────────────────────────────────────────────────┤
│                                                 │
│  【支出記録フォーム】                             │
│  ┌───────────────────────────────────┐         │
│  │ 日付: [2025-12-27         ▼]      │         │
│  │ カテゴリー: [食費          ▼]      │         │
│  │ 金額: [0            円]           │         │
│  │ メモ: [________________]          │         │
│  │        [  支出を追加  ]           │         │
│  └───────────────────────────────────┘         │
│                                                 │
│  【カテゴリー別集計】                             │
│  ┌───────────────────────────────────┐         │
│  │ 合計支出: ¥XX,XXX                 │         │
│  │                                   │         │
│  │ 📊 カテゴリー別内訳:               │         │
│  │ • 食費: ¥X,XXX                    │         │
│  │ • 交通費: ¥X,XXX                  │         │
│  │ • 娯楽費: ¥X,XXX                  │         │
│  │ • 光熱費: ¥X,XXX                  │         │
│  │ • 医療費: ¥X,XXX                  │         │
│  │ • その他: ¥X,XXX                  │         │
│  └───────────────────────────────────┘         │
│                                                 │
│  【支出一覧】                [同期] ボタン        │
│  ┌───────────────────────────────────┐         │
│  │ 2025-12-27  食費     ¥1,200      │         │
│  │ ランチ代                   [削除] │         │
│  ├───────────────────────────────────┤         │
│  │ 2025-12-26  交通費   ¥500        │         │
│  │ 電車賃                     [削除] │         │
│  └───────────────────────────────────┘         │
│                                                 │
└─────────────────────────────────────────────────┘
```

### 画面要素の詳細

1. **ヘッダーセクション**
   - アプリケーションタイトル
   - アイコン・サブタイトル

2. **支出記録フォーム**
   - 日付入力フィールド（type="date"）
   - カテゴリー選択ドロップダウン
   - 金額入力フィールド（type="number"）
   - メモ入力フィールド（type="text"、任意）
   - 送信ボタン

3. **カテゴリー別集計エリア**
   - 合計支出額表示
   - カテゴリー別支出リスト
   - 各カテゴリーの金額表示

4. **支出一覧エリア**
   - 全支出のリスト表示
   - 各項目に削除ボタン
   - Google Spreadsheetとの同期ボタン

## 入出力仕様

### 入力仕様

#### 支出記録フォーム

| 項目 | 型 | 必須 | 制約 | デフォルト値 |
|------|-----|------|------|-------------|
| 日付 | date | ○ | YYYY-MM-DD形式 | 当日の日付 |
| カテゴリー | select | ○ | 食費/交通費/娯楽費/光熱費/医療費/その他 | 食費 |
| 金額 | number | ○ | 正の整数、最小値: 1 | - |
| メモ | text | × | 最大200文字 | 空文字列 |

#### バリデーション

- 日付: 必須入力、有効な日付形式であること
- カテゴリー: 必須選択、定義されたカテゴリーから選択
- 金額: 必須入力、1以上の正の整数
- メモ: 任意入力、200文字以内

### 出力仕様

#### カテゴリー別集計

```javascript
{
  total: number,              // 合計支出額
  categories: {
    "食費": number,
    "交通費": number,
    "娯楽費": number,
    "光熱費": number,
    "医療費": number,
    "その他": number
  }
}
```

#### 支出一覧

```javascript
[
  {
    id: number,              // タイムスタンプベースのID (1696501234567)
    date: string,            // 日付（YYYY-MM-DD）
    category: string,        // カテゴリー
    amount: number,          // 金額
    memo: string             // メモ
  }
]
```

## 機能一覧

### 必須機能

#### 1. 支出記録機能

**機能ID**: F001  
**機能名**: 支出追加  
**説明**: 新しい支出をフォームから記録する

**処理フロー**:
1. ユーザーがフォームに情報を入力（日付、カテゴリー、金額、メモ）
2. 「支出を追加」ボタンをクリック
3. 入力値のバリデーション実施
4. バリデーション成功時、支出データを生成
   - IDはタイムスタンプ（`Date.now()`）を使用
5. localStorageに保存
6. **Google Spreadsheetに自動転送**（非同期処理）
7. 画面を更新（一覧と集計を再描画）
8. フォームをリセット

**データ保存先**:
- プライマリ: localStorage（即座に保存）
- セカンダリ: Google Spreadsheet（支出追加時に自動転送）

#### 2. 支出一覧表示機能

**機能ID**: F002  
**機能名**: 支出リスト表示  
**説明**: 登録された全支出を日付降順で一覧表示する

**処理フロー**:
1. localStorageまたはGoogle Spreadsheetから支出データを読み込み
2. 日付の降順でソート
3. 各支出をカード形式で描画
4. 各行に削除ボタンを表示

**削除機能**:
1. 削除ボタンをクリック
2. 該当IDのデータを配列から削除
3. localStorageを更新
4. 画面を再描画
5. （オプション）Google Spreadsheetにも反映

#### 3. カテゴリー別集計機能

**機能ID**: F003  
**機能名**: カテゴリー別集計表示  
**説明**: 支出をカテゴリー別に集計し、統計情報を表示する

**処理フロー**:
1. 全支出データを読み込み
2. カテゴリーごとに金額を集計
   - 食費
   - 交通費
   - 娯楽費
   - 光熱費
   - 医療費
   - その他
3. 合計金額を算出
4. 集計結果を表示

**集計ロジック**:
```javascript
function calculateStats(expenses) {
  const stats = {
    total: 0,
    categories: {
      "食費": 0,
      "交通費": 0,
      "娯楽費": 0,
      "光熱費": 0,
      "医療費": 0,
      "その他": 0
    }
  };
  
  expenses.forEach(expense => {
    stats.total += expense.amount;
    stats.categories[expense.category] += expense.amount;
  });
  
  return stats;
}
```

### データ同期機能

#### 4. Google Spreadsheet自動転送機能

**機能ID**: F004  
**機能名**: データ自動同期  
**説明**: 支出追加時に自動的にGoogle Spreadsheetへデータを転送・保存する

**処理フロー（自動転送）**:
1. 支出データがlocalStorageに保存された直後に実行
2. Google Apps Script（GAS）のWeb APIに新規データを送信
3. GASがSpreadsheetに1行追加（appendRow）
4. バックグラウンドで非同期処理（UI をブロックしない）
5. 成功/失敗をコンソールログに記録

**処理フロー（手動一括同期）**:
1. 「同期」ボタンをクリック（念のため全データを同期）
2. localStorage内の全支出データをGASに送信
3. GASがSpreadsheetの既存データをクリアして全データを再書き込み
4. 成功通知を表示

**同期方向**:
- ローカル → Spreadsheet（自動）: 支出追加時に即座に転送
- ローカル → Spreadsheet（手動）: 同期ボタンで一括転送
- Spreadsheet → ローカル: ページ読み込み時に自動取得（オプション）

## 使用技術

### 推奨技術スタック

#### フロントエンド
- **HTML5**: アプリケーションの構造
- **CSS3**: スタイリングとレスポンシブデザイン
- **JavaScript (ES6+)**: アプリケーションロジック

#### データ永続化
- **localStorage**: ブラウザローカルでの一時保存
- **Google Spreadsheet**: クラウドでのデータ永続化・バックアップ

#### バックエンド
- **Google Apps Script (GAS)**: SpreadsheetとのAPI連携

#### ホスティング・バージョン管理
- **GitHub**: ソースコード管理
- **GitHub Pages**: 静的サイトホスティング

### 技術詳細

#### HTML5
- セマンティックタグ（`<header>`, `<main>`, `<section>`）
- フォーム要素（`<form>`, `<input>`, `<select>`）
- メタタグ（viewport、charset、description）

#### CSS3
- CSS Variables（カスタムプロパティ）
- Flexbox / Grid Layout
- Transitions & Animations
- Media Queries（レスポンシブ対応）

#### JavaScript (Vanilla JS)
- DOM操作
- イベントハンドリング
- localStorage API
- Fetch API（GAS連携用）
- 日付処理（Date オブジェクト）

#### Google Apps Script
- doPost()でデータ受信
- Spreadsheet API でデータ書き込み
- Web アプリとして公開（URLエンドポイント提供）

### ブラウザ互換性

- Chrome 90+
- Firefox 88+
- Safari 14+
- Edge 90+

## データ構造

### localStorage仕様

#### キー名
```
expenses
```

#### データ形式（localStorage）
```javascript
// localStorage保存形式（JSON文字列）
{
  "expenses": [
    {
      "id": 1696501234567,           // タイムスタンプ（ミリ秒）
      "date": "2025-10-05",          // YYYY-MM-DD形式
      "category": "食費",            // カテゴリー名
      "amount": 800,                 // 金額（整数）
      "memo": "ランチ"               // メモ（文字列）
    },
    {
      "id": 1696587634567,
      "date": "2025-10-06",
      "category": "交通費",
      "amount": 300,
      "memo": "電車代"
    }
  ]
}
```

### データモデル詳細

#### Expense オブジェクト

| フィールド | 型 | 必須 | 説明 |
|-----------|-----|------|------|
| id | number | ○ | タイムスタンプ（`Date.now()`の返り値） |
| date | string | ○ | 支出日（ISO 8601形式: YYYY-MM-DD） |
| category | string | ○ | カテゴリー（食費/交通費/娯楽費/光熱費/医療費/その他） |
| amount | number | ○ | 金額（正の整数、単位: 円） |
| memo | string | × | メモ（最大200文字、空文字列可） |

### カテゴリー定義

```javascript
const CATEGORIES = [
  "食費",
  "交通費",
  "娯楽費",
  "光熱費",
  "医療費",
  "その他"
];
```

### localStorage操作関数

#### 保存
```javascript
function saveExpenses(expenses) {
  const data = { expenses: expenses };
  localStorage.setItem('expenses', JSON.stringify(data));
}
```

#### 読み込み
```javascript
function loadExpenses() {
  const storedData = localStorage.getItem('expenses');
  if (!storedData) return [];
  
  try {
    const data = JSON.parse(storedData);
    return data.expenses || [];
  } catch (error) {
    console.error('データ読み込みエラー:', error);
    return [];
  }
}
```

#### 新規追加（自動スプレッドシート転送付き）
```javascript
function addExpense(expense) {
  const expenses = loadExpenses();
  expense.id = Date.now();  // タイムスタンプでID生成
  expenses.push(expense);
  saveExpenses(expenses);
  
  // Google Spreadsheetに自動転送
  syncToSpreadsheet(expense);
}
```

#### 削除
```javascript
function deleteExpense(id) {
  const expenses = loadExpenses();
  const filtered = expenses.filter(exp => exp.id !== id);
  saveExpenses(filtered);
}
```

### Google Spreadsheet連携

#### Spreadsheetデータ構造

| A列 | B列 | C列 | D列 | E列 |
|-----|-----|-----|-----|-----|
| ID | 日付 | カテゴリー | 金額 | メモ |
| 1696501234567 | 2025-10-05 | 食費 | 800 | ランチ |
| 1696587634567 | 2025-10-06 | 交通費 | 300 | 電車代 |

#### GAS Web API仕様

**エンドポイント**: デプロイ後のGAS Web App URL

**リクエスト形式（新規追加時の自動転送）**:
```javascript
// 単一のexpenseデータをPOST
function syncToSpreadsheet(expense) {
  const GAS_URL = 'YOUR_GAS_WEB_APP_URL'; // デプロイ後のURL
  
  fetch(GAS_URL, {
    method: 'POST',
    mode: 'no-cors',
    headers: {
      'Content-Type': 'application/json',
    },
    body: JSON.stringify({
      action: 'add',
      expense: expense
    })
  })
  .then(() => console.log('Spreadsheetに転送成功'))
  .catch(error => console.error('転送エラー:', error));
}
```

**リクエスト形式（手動一括同期）**:
```javascript
// 全データを一括POST
function syncAllToSpreadsheet() {
  const expenses = loadExpenses();
  const GAS_URL = 'YOUR_GAS_WEB_APP_URL';
  
  fetch(GAS_URL, {
    method: 'POST',
    mode: 'no-cors',
    headers: {
      'Content-Type': 'application/json',
    },
    body: JSON.stringify({
      action: 'sync_all',
      expenses: expenses
    })
  })
  .then(() => alert('全データを同期しました'))
  .catch(error => alert('同期エラー: ' + error));
}
```

**GASコード例（自動転送 + 手動同期 対応版）**:
```javascript
function doPost(e) {
  try {
    const data = JSON.parse(e.postData.contents);
    const sheet = SpreadsheetApp.getActiveSpreadsheet().getActiveSheet();
    
    // ヘッダーが未設定の場合は設定
    if (sheet.getLastRow() === 0) {
      sheet.appendRow(['ID', '日付', 'カテゴリー', '金額', 'メモ']);
    }
    
    if (data.action === 'add') {
      // 新規追加：1件のデータを追加
      const expense = data.expense;
      sheet.appendRow([
        expense.id,
        expense.date,
        expense.category,
        expense.amount,
        expense.memo
      ]);
      
    } else if (data.action === 'sync_all') {
      // 一括同期：既存データをクリアして全データを再書き込み
      const lastRow = sheet.getLastRow();
      if (lastRow > 1) {
        sheet.deleteRows(2, lastRow - 1);
      }
      
      data.expenses.forEach(expense => {
        sheet.appendRow([
          expense.id,
          expense.date,
          expense.category,
          expense.amount,
          expense.memo
        ]);
      });
    }
    
    return ContentService.createTextOutput(
      JSON.stringify({ success: true })
    ).setMimeType(ContentService.MimeType.JSON);
    
  } catch (error) {
    return ContentService.createTextOutput(
      JSON.stringify({ success: false, error: error.toString() })
    ).setMimeType(ContentService.MimeType.JSON);
  }
}
```

**GASデプロイ設定**:
1. Google Apps Scriptエディタで上記コードを作成
2. 「デプロイ」→「新しいデプロイ」
3. 種類: ウェブアプリ
4. 実行ユーザー: 自分
5. アクセスできるユーザー: 全員
6. デプロイ後、Web App URLをコピーしてJavaScriptコードに設定
```

## GitHub Pages デプロイ

### リポジトリ構成

```
expense-tracker/
├── index.html          # メインHTMLファイル
├── style.css           # スタイルシート
├── app.js              # JavaScriptロジック
├── README.md           # プロジェクト説明
└── .gitignore          # Git除外設定
```

### デプロイ手順

1. **リポジトリ作成**
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   ```

2. **GitHubにプッシュ**
   ```bash
   git remote add origin https://github.com/username/expense-tracker.git
   git push -u origin main
   ```

3. **GitHub Pages設定**
   - リポジトリの Settings → Pages
   - Source: main branch
   - Save

4. **アクセス**
   - URL: `https://username.github.io/expense-tracker/`

## セキュリティ・パフォーマンス

### セキュリティ考慮事項
- ユーザー入力のサニタイゼーション（XSS対策）
- localStorageのデータは平文保存（機密情報は保存しない）
- GAS Web App URLは環境変数化（コミット禁止）

### パフォーマンス最適化
- 支出件数が多い場合の表示制限（最新100件など）
- 非同期データ同期（UI をブロックしない）

### エラーハンドリング
- localStorage利用不可能時の対応
- ネットワークエラー時の同期失敗通知
- データ破損時の復旧処理

## 今後の拡張可能性

- データエクスポート機能（CSV/JSON）
- 月次・年次レポート機能
- 予算設定・アラート機能
- グラフ表示（Chart.js等の導入）
- 編集機能の追加
- カテゴリーのカスタマイズ機能
